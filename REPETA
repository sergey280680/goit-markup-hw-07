создал такой миксин и нужно передать три значения 
    background-color,
    color,
    box-shadow


@mixin transition(
    $property,
    $time: 250ms,
    $cubic-bezier: cubic-bezier(0.4, 0, 0.2, 1)
) {
    transition: $property $time $cubic-bezier;
}


подскажи как передать сразу три значения в @include